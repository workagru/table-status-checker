# secrets — never commit (gmail_send.py holds the Gmail app password; use gmail_send.example.py)
gmail_send.py
secrets.local.json
secrets.local.*
# per-machine paths (absolute watcher_data, etc.) — see config.example.json
config.local.json
# consolidated local credentials file (shared with agents, never committed)
CREDENTIALS.local.md
*.local.md
*credentials*
*CREDENTIALS*
# service-account / API keys (the real ones live OUTSIDE the repo)
*service-account*.json
sheets-tool*.json
*-key.json

# generated / transient
# *_filled.py = probe with the work-list baked in (built by the generator)
*_filled.py
/tmp/
logs/
__pycache__/
*.pyc
.DS_Store
