# secrets — never commit (gmail_send.py holds the Gmail app password; use gmail_send.example.py)
gmail_send.py
secrets.local.json
secrets.local.*
# service-account / API keys (the real ones live OUTSIDE the repo)
*service-account*.json
sheets-tool*.json
*-key.json

# generated / transient
# *_filled.py = probe with the work-list baked in (built by the generator)
*_filled.py
/tmp/
__pycache__/
*.pyc
.DS_Store
